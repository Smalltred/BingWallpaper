# BingWallpaper 线上配置（部署包预置）
#
# 本文件是**模板**：tools/package.mjs 读它、加上一段说明头，作为包内 .env 打进去。
# 想改「新打出来的包」的默认配置，改这里；想改「已经部署上线的那一份」，改服务器上
# 站点根目录里的 .env（改完不需要重启 PHP）。
#
# 提交进仓库是刻意的：线上默认配置应当能被 review、能有历史，而不是藏在构建脚本里。

# 生产环境。会同时影响三件事：CORS 收紧、trust proxy 默认信任 1 层反代、docs 内嵌缓存。
APP_ENV=production

# sitemap.xml / robots.txt 里的站点根地址（不带结尾斜杠）。
SITE_ORIGIN=https://bing.hecady.com

# 壁纸库数据集目录。相对路径按「项目根」解析 —— dataset 与 app/ public/ 同级，
# 在 Web 根之外，所以既不会被 HTTP 直接下载到，也不会撞面板的 open_basedir 限制。
BINGIMAGES_DIR=dataset

# 反向代理层数：宝塔 nginx → php-fpm 是 1 层。填错的表现是所有访客共用一个限流额度，很快 429。
TRUST_PROXY=1
